#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    
    int n,i=1,j=0;
    scanf("%d",&n);
   while(i<=n){
         ++j;
       i=j*j;
       if(i<=n)
        printf("%d ",i);
     
    }
    return 0;
}
